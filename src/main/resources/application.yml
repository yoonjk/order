server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_DB}:5432/postgres
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
    
# username : labNN
academy:
  username: ${ACADEMY_USER}
  
mybatis:
  config-location: classpath:mybatis-config.xml

product:
  url: http://${PRODUCT}:8081/products
      
stock:
  url: http://${STOCK}:8081/stocks
  
payment:
  url: http://${PAYMENT}:8082/payments  
